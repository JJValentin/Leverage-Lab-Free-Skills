# Handoff (template; use the existing coordinator's fields)

Task ID and authoritative reference: <required>.
Owner/claim/lease: <revalidate in coordinator>.
Outcome and acceptance: <result and evidence>.
Project scope and protected constraints: <relevant only>.
Observed state and revision/time: <not a new claim of current state>.
Completed actions and receipts: <facts, including uncertain outcomes>.
Artifacts and versions/digests: <canonical references>.
Unresolved blockers or decisions: <decision ID and authorized recipient>.
Next action: <one executable step>.
Stop/escalation condition: <limits>.

Optional hub project ID, item ID and observed revision: <references>.
This handoff summarizes public work state and evidence, not hidden reasoning.
On resumption, re-read canonical task/approval state before proceeding.

Optional model observation: <contract/model versions, receipt ref, input source revisions, expiry>.
This is a prediction, not an approval or current task fact. Revalidate or discard after source changes.
