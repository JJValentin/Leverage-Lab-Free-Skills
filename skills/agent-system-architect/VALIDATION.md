# Agent System Architect 0.4.0: validation scope

Executed September 19, 2026 in a local Linux/Python environment. This update concerns interaction design, setup instructions, templates and evaluation specifications. No user project, private memory, native task state, provider credentials or live agent configuration was changed by these checks.

## Executed for this update

**22 repository-local checks passed** with:

```sh
python3 -B -m unittest discover -s tests -p 'test_experience.py' -v
```

Coverage: version metadata; local links; JSON parsing; 14 defined collaboration rules; 40 unique, complete scenario specifications; coverage of routine/critical and contrasting cases; non-deployed template defaults; unconfigured controls/scheduling; authority/confidence separation; adaptive response preferences; scoped memory/privacy defaults; unpopulated evaluation records; source IDs; unchanged decision-helper bytes; and the offline shadow-only demo.

These are package and fixture checks. They do not establish that an agent follows a rule or that a person finds the system usable. The tests deliberately assert that every behavioral scenario remains `not_run`.

**60 archived decision-helper regression tests also passed**, run from the supplied v0.3 development archive against the unchanged helper and equivalent parsed decision fixtures. That archived test suite is not added to this PR by the experience update. The helper's Git blob remains `5d43a33e813bbea4ca0f78b7291bd73f1196b1a4`. No new live Jev semantics are claimed.

Python syntax and the new JSON assets were checked locally. No new third-party dependency is required for the repository-local checks. The new source register distinguishes author book summaries, research abstracts, guidance, standards text and vendor engineering reports.

## Not executed or established

The **40 human-agent scenarios are specified, not run** against a model. No live Hermes, OpenClaw, pi, Claude Code or Codex behavior test; no native approval/cancellation/scheduler integration; no Jev API call or calibration; no end-user study; no accessibility conformance audit; no latency/cost/productivity benchmark; no independent security audit; and no production deployment.

The prior 51 Project Hub/installer tests and seven browser checks were not rerun in this experience update. Project Hub runtime files are not modified. Earlier validation claims are historical and must not be presented as new evidence.

## What publication means

Publishing the skill and templates updates repository content, not the user's active harness. The collaboration policy must be deliberately merged into the verified instruction scope, with local preferences preserved, then tested in that environment. The experience contract is not native configuration and its control fields default unsupported. The run template contains no fabricated observations.

GitHub branch/PR state is verified separately after publication. This validation document is not itself a receipt for a commit, installed policy or successful user experience.
