# Project entrypoint (template, not automatically loaded)

Outcome and completion evidence: <one useful result>.
Scope and non-goals: <limits>.
Accountable human: <owner>. Current work owner: read the canonical task.

Canonical project record: <reference>.
Task coordinator: <reference and supported read/claim method>.
Artifacts: <approved location>. Decisions/approvals: <canonical location>.
Relevant skills: <select only those needed>.

Optional hub: <project>/hub.json. Read compact status first, then the specific item
and its canonical task. The HTML and task snapshot are derived, not execution authority.
Revalidate the current task, artifact version and approval before consequential action.

Resume with the durable checkpoint. Preserve uncertain external outcomes and reconcile
before retrying. Update the canonical task first and the hub presentation second.
End with the verified result, outstanding decision and one useful next action.

Place only the relevant portion in the harness's verified instruction entrypoint.
Do not duplicate a working project record or import all references at startup.

Optional decision contract pointers: <only for the current workflow>.
Providers default off. Predictions suggest; canonical sources and actual permissions decide.
Do not load all contracts at startup or let a classifier veto an explicit authorized route.
